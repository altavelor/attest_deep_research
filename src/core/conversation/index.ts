// Публичный API модуля core/conversation — модель отображаемого диалога и
// чистые редьюсеры сообщений/reasoning/цепочек. Внешние потребители импортируют
// `@core/conversation`. Пофайловый `export *` (обе части публичны).
//
// Инвариант: файлы ВНУТРИ модуля не импортируют этот баррель — только соседей
// через `./…`, иначе цикл (ловит `npm run depcruise`).

export * from "./model";
export * from "./reducers";
