// Публичный API доменного ядра core/agent — протокол моделей, типы и имена
// инструментов, агентный цикл. Внешние потребители импортируют `@core/agent`.
//
// Это kernel-модуль: каждый файл целиком публичен (типы/константы протокола, без
// приватной реализации), поэтому здесь уместен пофайловый `export *` — в отличие
// от курируемых модулей, где наружу выставляется лишь узкая часть. Коллизий имён
// между файлами нет (проверяет `tsc`).
//
// Инвариант: файлы ВНУТРИ модуля не импортируют этот баррель — только соседей
// через `./…`, иначе цикл (ловит `npm run depcruise`).

export * from "./protocol";
export * from "./tool";
export * from "./toolNames";
export * from "./toolPresentation";
export * from "./AgentLoop";
